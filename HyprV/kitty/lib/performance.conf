# Delay between screen updates (in miliseconds)
repaint_delay 10

# Delay before input from the program running in the terminal is processed (in milliseconds).
input_delay 3

# Sync Screen updates to the refresh rate of the monitor
sync_to_monitor yes
